import React from "react";
import ProfileAvatar from "../componenets/genericComponents/ProfileAvatar";

export const options = (headerTitle, user) => {
	return {
		headerMode: "screen",
		headerTitle: headerTitle,
		headerTitleAlign: "center",
	};
};
